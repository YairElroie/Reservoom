<Window x:Class="Reservoom.Views.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Error"
        Height="220" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">
    <Border Background="#FFF0F0" CornerRadius="10" Padding="30">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.2" ShadowDepth="0"/>
        </Border.Effect>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="✕"
                       FontSize="40"
                       Foreground="#F44336"
                       HorizontalAlignment="Center"
                       FontWeight="Bold"/>
            <TextBlock Text="Something Went Wrong"
                       FontSize="18"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,5"/>
            <TextBlock x:Name="MessageText"
                       Text="An error occurred. Please try again."
                       FontSize="13"
                       Foreground="#666666"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"/>
            <Button Content="OK"
                    Width="90"
                    Margin="0,20,0,0"
                    Padding="10,6"
                    Background="#F44336"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="OkButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Shadow"
                                            Background="#B71C1C"
                                            CornerRadius="6">
                                        <Border x:Name="Face"
                                                Background="#F44336"
                                                CornerRadius="6"
                                                Margin="0,0,0,4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Face" Property="Background" Value="#D32F2F"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Face" Property="Margin" Value="0,4,0,0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Border>
</Window>
